# Правила: frontend-engineering

Применяются при подключённом профиле `--project-rules standard` или явном
выборе владельца. Обязательные требования владельца не выдаются за стандарт
внешнего источника. Для существующего несовместимого проекта обозначь конфликт,
не меняй архитектуру без задачи на переход.

## Размещение и зависимости FSD

FSD обязателен для новых frontend-проектов этого профиля. `kebab-case` — отдельное
требование владельца: `goal-card.tsx`, `goal-card.test.tsx`. Символы сохраняют
синтаксис языка. Обязательные framework-имена и generated-файлы перечисляются
точечно; это не разрешение для произвольного PascalCase имени файла.

Слои сверху вниз: `app → pages → widgets → features → entities → shared`.
`processes` устарел. Не создавай пустые слои. `app` собирает приложение;
`pages` хранит сценарий экрана; `features` — повторяемое действие пользователя;
`entities` — модель предметного объекта; `shared` — основу без бизнес-правил.
Текущая документация не рекомендует добавлять widgets по умолчанию: экранную
композицию оставляй в pages, повторяемое действие с UI — в features.

Слайс — область продукта (`training-plan`), сегмент — назначение (`ui`, `api`,
`model`, `lib`, `config`). App и Shared содержат сегменты без бизнес-слайсов.
Импорты другого слайса направлены строго вниз; связи соседних features собирай
выше. Внутри собственного слайса разрешены прямые относительные импорты.

Публичный API экспортирует только поддерживаемые элементы. Не импортируй
внутренности соседнего слайса и собственный index из внутреннего файла:
последнее создаёт цикл. Не используй массовый `export *`. `@x` разрешён только
для явной связи entities, с API для конкретного потребителя. Для shared/ui
и shared/lib делай небольшие точки входа по компоненту/библиотеке.

Пример размещения (проектное применение правил):

```text
src/pages/goals/ui/goals-page.tsx
src/features/complete-goal/ui/complete-goal-button.tsx
src/features/complete-goal/model/complete-goal.ts
src/features/complete-goal/index.ts
src/entities/goal/model/goal.ts
src/entities/goal/index.ts
src/shared/ui/button/index.ts
```

`pages/goals` импортирует `features/complete-goal`; обратная зависимость запрещена.
`features/complete-goal` использует `entities/goal`, но не соседнюю feature.
При втором использовании одного поведения выдели компонент/функцию в доступный
потребителям слой. Не переносись в shared вместе с бизнес-зависимостью.
Проверяй граф импортов линтером, включая aliases, type imports и циклы.

Источники, проверены 2026-09-14:
[FSD Layers](https://fsd.how/docs/reference/layers/),
[Slices and segments](https://fsd.how/docs/reference/slices-segments/),
[Public API](https://fsd.how/docs/reference/public-api/).

## UI и поведенческие тесты

Владелец серверных данных — query/cache слой, формы — локальный сценарий.
Не дублируй вычисляемое состояние; обработай устаревший ответ и отмену запроса.
Проверь загрузку, пустые данные, отказ, повторную отправку и восстановление.
Тестируй действие через доступную пользователю роль/имя, изолируй тестовые
данные; ожидание состояния предпочтительнее фиксированной задержки.
Тест компонента подтверждает события и результат, E2E — критический путь.
Перед PR проверь keyboard/focus, responsive состояния, lint/types и coverage.
Релизную сборку запускай только по политике поставки, компиляцию для тестов — в CI.

Источник: [Playwright best practices](https://playwright.dev/docs/best-practices),
проверен 2026-09-14.

## Проверка тестами

Требование владельца: минимум 90% строк и ветвей исполняемого авторского кода
по затронутому приложению/независимому сервису; также по изменённому коду при
достоверной поддержке diff coverage. Не усредняй frontend и backend.
Измеряй и неисполненные файлы, а не только импортированные тестами. Исключения:
generated/vendor и неисполняемые декларации, по явному списку. Отсутствующий
отчёт — блокирующая непроверенность, нулевой знаменатель — N/A, не 100%.
CI обязан отклонять результат ниже порога. Если инструмент измеряет только
statements (например, стандартный Go coverage), не называй их branches/lines:
зафиксируй метрику и согласуй дополнительную проверку до заявления о прохождении.
Не понижай порог и не исключай сложную логику. Порог — политика проекта,
а не гарантия отсутствия ошибок и не требование внешней документации.
В отчёте укажи команду, версию инструмента, область, exclusions, проценты
и непроверенные сценарии. Тесты должны проверять поведение и значимые ошибки.

Пример для Vitest: включи `coverage.include: ['src/**/*.{ts,tsx}']`,
`coverage.thresholds: { lines: 90, branches: 90 }`, задай проверенные exclusions
и запускай с coverage. Это пример для совместимой версии, не конфигурация
любого стека. Источник: [Vitest coverage](https://vitest.dev/config/coverage), 2026-09-14.
